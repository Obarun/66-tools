# The mkdocs configuration is no longer built here. obdev/docs-builder writes
# it from its own base and this project's doc/doc.yml. All that remains is
# generating the markdown the site is made of.

subdir('docs')
